<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf8">
    <title>Sound PI</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Sound PI">
    <link href="/assets/app.css" rel="stylesheet">
    <!--[if lt IE 9]>
    <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body>

    <div class="container-fluid">

      <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="song_list">
        <thead>
          <tr>
            <th>Name</th>
            <th>Artist</th>
            <th>Album</th>
          </tr>
        </thead>
        <tbody>
          <% @songs.each do |song| %>
            <tr>
              <td><%= song.name %></td>
              <td><%= song.album.artist.name %></td>
              <td><%= song.album.name %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

    </div>

    <script src="/assets/app.js"></script>
    <script>
      $(document).ready(function(){
        $('#song_list').dataTable({
          "sDom": "<'row'<'span6'l><'span6'f>r>t<'row'<'span6'i><'span6'p>>",
          "iDisplayLength": 25
        });
      });
    </script>
  </body>
</html>
